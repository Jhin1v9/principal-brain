# 2026-09-25

## Diagnóstico API Kimi (09:3x, autor: Luna)
- Owner alertou: "é API da kimi.ai code console, não da Moonshot — o formato da env tá errada".
- Teste ao vivo das duas keys contra `LLM_BASE_URL` do .env:
  - `LLM_API_KEY` (elo 1): **403 — quota semanal (7-day usage limit) esgotada**; reseta no fim da janela, ou compra extra no https://www.kimi.com/membership
  - `KIMI_API_KEY_2` (elo 2): **200 OK**, respondeu `kimi-k2.6`
- Conclusão: formato do .env já está correto (api.kimi.com/coding/v1 + sk-kimi + kimi-k2.6 = console Kimi Code). Não é Moonshot, não há formato errado. A key 1 está só sem quota semanal; a cadeia de fallback (ADR-039/3 elos) cobre com a key 2.
- Keys 1 e 2 são distintas (fallback real, não duplicata).
