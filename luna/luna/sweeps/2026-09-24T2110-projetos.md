# Varredura de projetos — 2026-09-24 21:10 (+02:00)

Janela: sweep 21:00 → 21:10. autor: Luna (cron de varredura).

## Commits no luna-agent (1, de outra sessão Luna — ordem Jhino)

- **c4e8b2d** 20:58 Luna — **fix anti double-send da ponte Kimi Work**:
  - `whatsapp.message.received` com ponte na `kimiwork_inbox` não passa pelo model router
    (via canônica = varredura dedicada); exceção §8.1 (pedido explícito de humano) preservada
  - `KimiWorkAdapter`: sem timeout de operação (decreto Jhino) — poll com progresso a cada 2min;
    cap de 12h só como segurança do client lock
  - env: `KIMIWORK_LLM_TIMEOUT_MS=43200000`
  - testes: f21 (3 cenários da ponte) + f14 ajustado + unitários do adapter

## Notas

- `.env` alterado às 20:56 (outra sessão) — mas só timeout; `NEXO_API_*` do painel **inalterados**
  (login segue CredentialsSignin, ~4h30 de bloqueio).
- Commit com email de autor `luna@nexo-digital.local` (sessão manual), não o do cron.
- Serviços estáveis; /ready 200 há ~38min (restart #4 segue valendo).
