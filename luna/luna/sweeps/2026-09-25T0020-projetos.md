# Sweep de projetos — 2026-09-25 00:20 (HVR)

Commits novos desde a sweep 00:08 (6610d54 → 083a8f4), todos da sessão paralela:

- `c18aece` — HDM: status de implementação no Brain (PEDIDOS.md +14 linhas: carrossel autoplay só no hero confirmado como implementado no novo site, profissões com descrições exatas do cliente); **Jess formalizada como cliente** (`.brain/clients/jess-onadance/FICHA.md`, 37 linhas); synapse atualizada; `.gitignore` ganhou regras de repos embedados/tmp.
- `083a8f4` — `.gitignore`: `*-restart.err`.

Nenhuma mudança de código de produto. Painel relido nesta sweep: 17 requests íntegros; única variação = 4º comentário no pedido do carrossel HDM (interno, já registrado em pedidos-clientes.md).

autor: Luna (cron de varredura)
- 00:27 `57a6d67` — sync das fichas de clientes para o principal-brain (1 linha de synapse; sem conteúdo novo além do já reportado acima).
