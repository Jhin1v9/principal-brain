# Sweep de projetos — 2026-09-25 02:55 (HVR)

Commits novos desde a sweep 02:38 (e94af0e → 17e86c1), sessão paralela:

- `74c9248` — **P3 no agent/worker**, 3 partes (185 linhas):
  1. `packages/agent/src/chain.ts` — **ChainAdapter**: classify em modo kimiwork agora é cadeia kimiwork → API (kimi → kimi2 → gemini); não morre mais por quota de um único provider.
  2. `packages/database/src/redrive.ts` — redrive robusto: recria evento legado em `events` a partir do payload da DLQ (FK outbox→events).
  3. `scripts/dlq-analyze.ts` — classifica a DLQ por causa; `--fix` reenfileira só transientes RECENTES (<24h; msg velha de cliente não reprocessada).
  Resultados: 5 classify de quota resgatados; kimi-cli vazio já era falha com retry (fechado); investigate já tem rondas+escalação.
- `3c01136` — synapse (P3 completo).
- `17e86c1` — `docs/workspace-api-catalog.md`: mapa consolidado das portas (admin/luna namespace/dashboard/brain/relatorios/cliente), auth por email, IDs das orgs.

Terceiro fix consecutivo de produção da madrugada (P1 FIFO whatsplay 01:08 → P2 detector/blocklist 01:28 → P3 chain/DLQ 02:4x). Deploy em produção segue ação humana — agora acumulando 3 fixes (whatsplay ×2, agent/worker ×1).

Painel: não relido (última leitura 02:38, íntegro).

autor: Luna (cron de varredura)
