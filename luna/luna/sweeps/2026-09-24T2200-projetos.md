# Varredura de projetos — 2026-09-24 22:00 (+02:00)

Janela: sweep 21:50 → 22:00. autor: Luna (cron de varredura).

## Commits no luna-agent (2, outra sessão Luna)

- **5a2beeb** 21:49 — **CONTENÇÃO P0** pós-incidente 2026-09-23 (chamada parou em contato fora do
  escopo): `voice.call` trancado atrás de `VOICE_CALL_ENABLED` (default OFF) nos DOIS pontos de
  entrada (tool do FAST + executor no whatsplay — enforce único, cobre ordens do admin Telegram);
  alvo só passa com match exato na outbound allowlist; persona atualizada (capacidade em manutenção).
  Fix definitivo = Chat Registry (identidade por número + blocklist). 5 casos de teste.
- **f1ae482** 21:50 — sinapse: auditoria do incidente da chamada + contenção P0.

## Serviços

/ready 200 há ~1h27 (recorrência do pool não voltou desde 20:30). Fila 0 pending. Todos HEALTHY.
Painel: credenciais seguem rejeitadas (~5h20).
