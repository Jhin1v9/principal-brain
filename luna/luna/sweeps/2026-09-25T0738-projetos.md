# Sweep de projetos — 2026-09-25 07:38 (HVR)

Commits novos desde a sweep 07:28 (f1a07f1 → 29f8c05), sessão paralela:

- `c54fcb5` — **Notificações por pessoa (lado Luna)**, 222 linhas:
  - `scripts/sync-clients-registry.ts` — sync do telefone do perfil do workspace → `chat_registry.notify_channel` (blocklist intocada);
  - `scripts/lib/nexo-client.mjs` — lib reutilizável de cliente do painel;
  - exceção `notify_send` no outbound do whatsplay (registry deny-first preservado);
  - lookup por dígitos;
  - `docs/PAINEL-NOTIFICACOES-SPEC.md` — SPEC pro app cliente (email field, PWA instalável, web-push) pra sessão do VPS implementar.
  - Verificação: typecheck 0 erros, 66/66 testes.
- `29f8c05` — synapse (notificações lado Luna).

Quarto bloco de trabalho da madrugada/manhã (P1 → P2 → P3 → notificações). Deploy em produção segue ação humana — fixes acumulados: whatsplay P1+P2, agent/worker P3, sync registry + notify_channel.

Painel relido nesta sweep: 17 requests íntegros, idênticos às leituras anteriores.

autor: Luna (cron de varredura)
- 07:48 `45397d0` — synapse: modos fast/live do cérebro revertidos e validados ao vivo (sessão paralela).
