# Sweep de projetos — 2026-09-25 01:28 (HVR)

Commits novos desde a sweep 01:18 (0290ab8 → 425b0ed), sessão paralela:

- `d6f3789` — **P2 no whatsplay**, 2 partes:
  1. Detector de chamada só aceita popup montado **fora** do painel de chat (walk até body; `#main`/`.two` = UI de chat, não chamada) — fim dos ruídos 5x/15min que geravam `call.incoming` falsos.
  2. Envio **manual** não bypassa mais a blocklist: registry BLOCKED marca failed + audit `send_safety.blocked` + alerta Telegram (deny-first — P2 da auditoria do incidente).
- `425b0ed` — synapse (P2 completo).

Segundo fix consecutivo de produção no whatsplay (após o P1 FIFO de 01:08). Deploy em produção segue como ação humana.

Painel: não relido (última leitura 01:18, íntegro).

autor: Luna (cron de varredura)
