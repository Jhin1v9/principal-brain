# Varredura de projetos — 2026-09-24 22:20 (+02:00)

Janela: sweep 22:10 → 22:20. autor: Luna (cron de varredura).

## Commits no luna-agent (2, outra sessão Luna)

- **5ac505a** 22:08 — sinapse: chat registry entregue
- **087672c** 22:16 — **tool permanente de acesso ao painel** (`scripts/nexo-admin.mjs`): login
  NextAuth completo com refresh automático do cookie em `.brain/.vps_cookie`; chamada autenticada
  em qualquer `/nexo/api/admin/*`. Sessão paralela **testou variações de username** — todas
  CredentialsSignin. Conclusão confirmada: falta a senha correta (humano).

## Serviços

✅ Watchdog voltou a HEALTHY (chrome UNKNOWN foi transitório, conforme diagnóstico da sweep 22:10).
/ready 200 há ~1h47. Fila 0 pending. Todos HEALTHY.

## Painel

Bloqueio **~5h40**, causa confirmada: credenciais inválidas (nem variações de username funcionam).
Tool pronto (`scripts/nexo-admin.mjs`) — assim que a senha correta entrar no `.env`, a varredura
volta automaticamente.
