# Sweep de projetos — 2026-09-25 08:48 (UTC+2)

## Commit novo de terceiros (sessão paralela)

- `563598e` — **FIX P0 chamadas** (whatsplay): o discriminador do popup de chamada estava CEGO.
  Causa raiz: o seletor `.two` é o wrapper de **todo o app** WhatsApp Web, não do popup — o
  detector P2 v1 via 2 botões "visíveis" durante o ring e não detectava nada.
  Correção (`apps/whatsplay/src/browser.ts`, +8/−4): só aceita botões dentro de `#main`/`#pane-side`.
  Validado com probe ao vivo durante ring.

## Contexto

Faz parte da linha P1+P2 do whatsplay (FIFO + detector de chamada + blocklist manual, commits
`ddd5e4f`, `d6f3789`). Tudo ainda em **dev** — deploy em produção segue como ação humana pendente.

## Saúde no momento do sweep

- health/ready 200; heartbeats frescos (orchestrator, worker, watchdog HEALTHY, whatsplay LOGGED_IN).
- Fila `kimiwork_inbox`: mesmo item `f567f83f` pending desde 05:58 UTC (~2h45m) — alerta já
  enviado às 08:28, não repetido. Ação humana: verificar automação Geral.
