# Varredura de projetos — 2026-09-24 22:10 (+02:00)

Janela: sweep 22:00 → 22:10. autor: Luna (cron de varredura).

## Commits no luna-agent (1, outra sessão Luna)

- **bcbf2ba** 22:07 — **Chat Registry (fix definitivo P0, migração 012)**: tabela gorda
  (chat_key=numero/jid, nome canônico, papel allowed/blocked, notify_channel), deny-first fail-closed.
  `registryLookup/Check/Upsert` no @luna/database (testes 6/6). Enforce nos 2 pontos de voice.call.
  Seed: Pai/Abner/Produção allowed, namorada BLOCKED (nota do incidente 23/09). Typecheck 0, suites verdes.

## Serviços

/ready 200 há ~1h37 (recorrência do pool não voltou desde 20:30 — considerar monitoramento encerrado,
reativar se voltar). Fila 0 pending. Worker/whatsplay/orchestrator HEALTHY.

⚠️ watchdog reportou **DEGRADED** no ciclo 20:07 — causa: `chrome=UNKNOWN`, probe intermitente
(mesmo padrão já ocorrido 12:36 e 13:05, com ciclos HEALTHY entre eles). Não é incidente novo;
re-checar no próximo sweep. Se persistir 3+ ciclos, investigar o probe de chrome do watchdog.

## Painel

Login segue `CredentialsSignin` (~5h30). `.env` inalterado nas credenciais.
