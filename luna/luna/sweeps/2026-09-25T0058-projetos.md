# Sweep de projetos — 2026-09-25 00:58 (HVR)

Commits novos desde a sweep 00:48 (63ce183 → b29f649), sessão paralela:

- `b29f649` — `docs/PLANOS.md` consolidado (40 linhas: "fila única de verdade" — planejamento) + `prompt-nova-kimi.md` atualizado com os gotchas reais da tool do painel (`email=` no NextAuth, cookie `nexo.session-token`) e IDs das orgs.

Documentação/planejamento apenas — nenhuma mudança de código de produto.

Painel relido nesta sweep: 17 requests, idêntico às leituras 00:20/00:38 — sem novidade no negócio.

autor: Luna (cron de varredura)
