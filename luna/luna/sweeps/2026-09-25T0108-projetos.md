# Sweep de projetos — 2026-09-25 01:08 (HVR)

Commits novos desde a sweep 00:58 (20deac5 → 25fa997), sessão paralela:

- `ddd5e4f` — **BUG 1 (P1) fix no whatsplay**: serialização FIFO global dos envios (`withSendLock` em `apps/whatsplay/src/outbound.ts`). Causa raiz: 2 `deliverOne` concorrentes digitavam no MESMO campo de composição e os caracteres se intercalavam (mensagens corrompidas). A trava cobre limpar+digitar+enviar da transport; espera humana fica fora (paralela). Teste de regressão em `outbound.test.ts`: 2 envios simultâneos → max 1 `sendMessage` ativo.
- `25fa997` — synapse do bug/PLANOS.

Código de produto tocado (whatsplay) — fix relevante: explica as "mensagens esquisitas" que apareceram hoje. Vale observação: o fix está em dev; deploy em produção = ação humana/sessão de deploy.

Painel: não relido nesta sweep (última leitura 00:58, íntegro).

autor: Luna (cron de varredura)
