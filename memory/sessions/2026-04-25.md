# 2026-04-25

## Supreme Analysis
- Applied the `.brain` testing prompt in live mode against `cliente`, `kds`, and `kiosk`.
- Confirmed same Supabase host across the three live bundles: `dproxlygtabihfhtxdvm.supabase.co`.
- Confirmed a real `cliente` order reaches KDS and status changes return to the same-device client session.
- Confirmed a real `kiosk` order reaches KDS with `TPV Mesa` origin.

## Findings
1. Fresh browser login by phone is still local-first and fails for existing remote customers.
   - Evidence in `QuickRegister.tsx` login flow and `authMock.ts` local-only lookup.
2. Client has missing static product assets returning `404` in production.

## Artifacts
- `artifacts/supreme-analysis-2026-04-24T23-06-45-526Z`
- `artifacts/supreme-analysis-final-2026-04-25.md`

## Fixes Applied
- Added remote-only phone lookup helper in `apps/cliente/src/lib/customerProfile.ts`.
- Updated `QuickRegister` login mode to fall back to remote customer lookup when local mock storage is empty.
- Removed automatic `.webp` source generation from `packages/shared/src/components/OptimizedImage.tsx` to stop production `404` asset requests.

## Validation
- `npm run build:cliente` passed.
- `npm test` passed (`81/81`).
- Deployed `cliente` live.
- Fresh-browser login by phone now works for existing remote customer phone.
- No more product asset `404` errors in live client verification.
- Verification artifact: `artifacts/cliente-fix-verify-2026-04-25T06-10-20-880Z`.

## Network Reality Check
- Direct `create_order` via Supabase REST RPC returned `200` and generated order `#015`.
- That same direct API order appeared in KDS, proving the LAN/shared-network hypothesis is false for order transport.
- Fresh evidence artifact: `artifacts/network-reality-2026-04-25T06-15-22-240Z`.

## Notification Reality Check
- Client has realtime sync, but no real push notification plumbing yet.
- `apps/cliente/vite.config.ts` does not register PWA plugin.
- `apps/cliente/src/main.tsx` does not register any service worker.
- No code found for `showNotification`, `Notification.requestPermission`, or push subscription handling.
- Conclusion: when kitchen marks order `listo`, the UI can update while the app is open, but background/device push like `SEU SORVETE ESTA PRONTO` is not implemented yet.

## Real Push Implementation
- Implemented real web push plumbing for the client PWA.
- Added `public/sw-cliente.js` and client-side registration/subscription helper in `apps/cliente/src/lib/pushNotifications.ts`.
- `apps/cliente/src/main.tsx` now registers the service worker on boot.
- `apps/cliente/src/ClienteApp.tsx` now silently re-syncs push subscription for returning customers when permission is already granted.
- `apps/cliente/src/pages/CarrinhoPage.tsx` now attempts to subscribe/register push right after successful checkout, while user intent is highest.

## Supabase Push Backend
- Added migration `supabase/migrations/20260425073000_add_push_subscriptions.sql`.
- Added edge function `register-push-subscription`.
- Added edge function `notify-order-ready` using VAPID web push.
- `packages/shared/src/realtime/client.ts` now invokes `notify-order-ready` when KDS marks an order as `listo`.

## Infra / Deploy Notes
- Deployed `register-push-subscription`, `notify-order-ready`, `send-receipt`, and `create-payment-intent` as public edge functions with `--no-verify-jwt` because the app uses publishable-key access rather than authenticated user JWTs.
- Set custom Supabase secrets successfully via env file: `WEB_PUSH_PUBLIC_KEY`, `WEB_PUSH_PRIVATE_KEY`, `PUSH_CONTACT_EMAIL`.
- Deployed updated `cliente` and `kds` live.
- `cliente-pearl.vercel.app` now serves `sw-cliente.js` and the live client bundle includes the public push key plus the subscription registration flow.
- `kds-one.vercel.app` live bundle now includes the `notify-order-ready` invocation path.

## Validation Reality
- Backend proof passed: direct API order creation + status update to `listo` + `notify-order-ready` function returned `200` with `ok: true`.
- Proof artifact: `artifacts/push-backend-proof-2026-04-25T06-47-00-322Z`.
- Client push last-mile automation is still blocked in Playwright/Chromium because this environment forces `Notification.permission = denied` even on secure origin with service worker and granted permissions API.
- Push live artifacts showing this limitation: `artifacts/push-live-2026-04-25T06-34-47-614Z` and `artifacts/push-live-2026-04-25T06-38-24-465Z`.
- Conclusion: code and backend are now in place; final proof of actual device notification still requires one human-run browser/phone session.

## Important Operational Note
- Running `supabase db push` reapplied `20260423224221_fix_missing_functions.sql` before the new push migration because of the remote migration state, and that migration includes truncation/seed logic. Treat the demo database as refreshed from that moment.
- Follow-up hardening: switched client edge-function calls in `CarrinhoPage.tsx` from raw `fetch` to `supabase.functions.invoke` for `create-payment-intent` and `send-receipt`.
- Confirmed live cliente bundle now includes `register-push-subscription`, `create-payment-intent`, and `send-receipt` invocation paths.
- Confirmed live register function is active and reachable after redeploy; smoke invoke returned `200`.
- Smoke artifact for registered-subscription path: `artifacts/push-smoke-2026-04-25T06-49-53-922Z`.

## Ingestao de contexto externo
- Li a secao `ANALISE DA KIMI 2.6` em `.brain/context.md` e vou usá-la como insumo comparativo na consolidacao.
- Regra de consolidacao: manter separacao entre `relato da Kimi` e `confirmacao live do Codex` para evitar falso positivo.
- Autor desta anotacao: CODEX.

## ANALISE DA KIMI 2.6 — Complemento de contexto visual
- Novo asset de logo criado pelo usuario e localizado em `public/assets/logo/ChatGPT Image 25 abr 2026, 08_46_42.png`.
- Esta anotacao foi escrita por CODEX para garantir rastreabilidade do asset novo.
- Sobre a questao de alinhamento/uso da logo no PWA: a Kimi ja havia registrado o tema `Logo PWA nao centralizada` em `.brain/context.md`; portanto aqui eu apenas referencio que o asset fonte agora existe no projeto.
- Regra de consolidacao: se a analise final usar a nova logo, comparar implementacao visual com este asset fonte antes de concluir desalinhamento ou regressao.

## ANALISE DA KIMI 2.6 — Complemento CODEX sobre telefone
- Novo direcionamento do usuario: remover a friccao de `+34` no input e tratar a organizacao do numero como responsabilidade forte do backend.
- Checagem de consolidacao: a Kimi nao havia descrito este ponto de forma explicita; portanto este complemento entra como anotacao nova de CODEX, nao como repeticao.

### Decisao de produto
- Em Espanha, o cliente nao deve ser forcado a inserir `+34` manualmente.
- O sistema deve aceitar formatos comuns escritos pelo usuario, como:
  - `624529442`
  - `624 529 442`
  - `624 52 94 42`
  - `+34 624 529 442`

### Decisao tecnica
- A normalizacao canonica do telefone deve acontecer no backend para evitar divergencia entre:
  - login por telefone
  - historico do cliente
  - perfil do cliente
  - notificacoes e verificacao
- Objetivo: variantes visuais do mesmo numero devem convergir para uma unica identidade telefonica.

### Backlog solicitado pelo usuario
- Adicionar depois em `Configuracoes` uma opcao `Verifique seu telefone`.
- Fluxo desejado: enviar mensagem para confirmar posse do numero por seguranca.
- Estado: apenas anotado no brain como trabalho futuro; ainda nao implementar.
- Autor desta anotacao: CODEX.

## ATUALIZACAO DE ESTADO — CODEX

### Bug 2 — Alergenos com traducao errada
- Item herdado da `ANALISE DA KIMI 2.6`.
- Correcao aplicada em `apps/cliente/src/components/ProductDetailModal.tsx`.
- Mudanca: o modal agora usa `nomeAlergeno(aviso.alergeno, locale)` em vez da chave interna raw.
- Resultado esperado: em espanhol passa a exibir `Huevos`, `Leche` etc. no mesmo idioma da interface.
- Validacao:
  - `npm run build:cliente` passou em 2026-04-25;
  - arquivo de implementacao confirmado.
- Status vigente: `CORRIGIDO`.
- Autor desta atualizacao: CODEX.

### Bug 8 — Historial por telefone nao funciona
- Item herdado da `ANALISE DA KIMI 2.6`.
- Correcao aplicada em `packages/shared/src/realtime/client.ts`.
- Mudanca:
  - o fallback standalone agora persiste `customerId: payload.checkout.customerId || null`;
  - o fallback standalone agora propaga `origem: payload.checkout.origem || 'tpv'`.
- Resultado esperado: o filtro de `PedidosPage` volta a encontrar os pedidos do mesmo cliente por `customerId`/telefone sem perder identidade no modo standalone.
- Validacao:
  - `npm run build:cliente` passou em 2026-04-25.
- Status vigente: `CORRIGIDO`.
- Autor desta atualizacao: CODEX.

### Bug 5 — KDS mostra "TPV Mesa" em vez de "TPV"
- Item herdado da `ANALISE DA KIMI 2.6`.
- Correcao aplicada em `apps/kds/src/KDSApp.tsx` com apoio da propagacao de origem em `packages/shared/src/realtime/client.ts`.
- Mudanca:
  - badge do KDS mostra `TPV` para pedidos `tpv` e `kiosk`;
  - badge `App Cliente` continua apenas para `pwa`;
  - filtro `TPV` do KDS agora inclui `tpv` e `kiosk`.
- Validacao:
  - `npm run build:kds` passou em 2026-04-25;
  - `npm run build:cliente` passou apos ajuste no shared.
- Status vigente: `CORRIGIDO`.
- Autor desta atualizacao: CODEX.

### Bug 1 — Textos da animacao de pagamento com underscores
- Item herdado da `ANALISE DA KIMI 2.6`.
- Correcao aplicada nos arquivos `packages/shared/src/i18n/es.ts`, `ca.ts`, `pt.ts` e `en.ts`.
- Mudanca:
  - adicionadas as chaves de status usadas por `apps/cliente/src/components/pagamento/ProcessandoPagamento.tsx`;
  - isso elimina a exibicao de placeholders/keys raw durante a animacao de pagamento.
- Validacao:
  - `npm run build:cliente` passou em 2026-04-25.
- Status vigente: `CORRIGIDO`.
- Autor desta atualizacao: CODEX.

### Bug 3 — "Pular tutorial" pouco visivel
- Item herdado da `ANALISE DA KIMI 2.6`.
- Correcao aplicada em `apps/cliente/src/components/onboarding/InteractiveTutorial.tsx`.
- Mudanca:
  - o botao de skip saiu de um texto quase invisivel para um CTA leve, com borda, fundo e contraste adequado;
  - isso melhora descobribilidade sem poluir a interface.
- Validacao:
  - `npm run build:cliente` passou em 2026-04-25.
- Status vigente: `CORRIGIDO`.
- Autor desta atualizacao: CODEX.

### Bug 6 — Setas de voltar em locais errados
- Item herdado da `ANALISE DA KIMI 2.6`.
- Correcao concluida em `apps/cliente/src/ClienteApp.tsx` e `apps/cliente/src/pages/PedidoDetalhesPage.tsx`.
- Mudanca:
  - a seta do header principal foi escondida quando nao existe `onBack`;
  - o titulo ficou centralizado sem manter um botao morto no topo;
  - a tela de detalhes trocou a seta quadrada antiga por um retorno textual `Volver a pedidos`.
- Validacao:
  - `npm run build:cliente` passou em 2026-04-25.
- Status vigente: `CORRIGIDO`.
- Autor desta atualizacao: CODEX.

### Bug 7 — Logout em Configuracoes nao funciona
- Item herdado da `ANALISE DA KIMI 2.6`.
- Correcao aplicada em `apps/cliente/src/pages/ConfigPage.tsx`.
- Mudanca:
  - foi adicionada limpeza explicita da sessao persistida do cliente;
  - o fluxo remove `tpv-onboarding-completed` e evita que o usuario reapareca no reload por estado residual.
- Validacao:
  - `npm run build:cliente` passou em 2026-04-25.
- Status vigente: `CORRIGIDO EM CODIGO`.
- Observacao:
  - ainda vale uma checagem manual simples no browser para carimbar o comportamento live.
- Autor desta atualizacao: CODEX.

### Bug 4 — Logo PWA nao centralizada
- Item herdado da `ANALISE DA KIMI 2.6`.
- Correcao aplicada em `apps/cliente/src/ClienteApp.tsx`.
- Mudanca:
  - o header passou a usar a logo real do projeto;
  - a estrutura central agora trabalha com imagem centralizada e `object-contain`, em vez de depender apenas do texto `Tropicale`.
- Validacao:
  - `npm run build:cliente` passou em 2026-04-25.
- Status vigente: `CORRIGIDO EM CODIGO`.
- Observacao:
  - ainda vale uma checagem visual manual para fechar o item como totalmente confirmado no device real.
- Autor desta atualizacao: CODEX.

## ATUALIZACAO DE ESTADO — CODEX sobre telefone na Espanha
- O complemento de telefone no `.brain` deixou de ser apenas diretriz e virou implementacao real nesta rodada.
- Mudanca central:
  - o sistema aceita telefone espanhol com ou sem `+34` e converte tudo para o formato canonico de 9 digitos.
- Superficie coberta:
  - cadastro/login rapido;
  - configuracoes;
  - Bizum/pagamento;
  - lookup remoto e upsert de cliente;
  - fallback local/shared;
  - funcoes SQL para `customers` e `orders`;
  - migration para normalizar dados existentes e `push_subscriptions`.
- Arquivos principais:
  - `packages/shared/src/lib/phone.ts`
  - `apps/cliente/src/components/onboarding/QuickRegister.tsx`
  - `apps/cliente/src/pages/ConfigPage.tsx`
  - `apps/cliente/src/components/pagamento/PagamentoModal.tsx`
  - `apps/cliente/src/lib/customerProfile.ts`
  - `packages/shared/src/realtime/client.ts`
  - `supabase/migrations/20260425111500_normalize_spanish_phones.sql`
- Validacao:
  - `npm run build:cliente` passou em 2026-04-25.
- Status vigente: `IMPLEMENTADO EM CODIGO`.
- Backlog preservado:
  - `Configuracoes > Verifique seu telefone` continua como proximo degrau, agora em cima de uma identidade telefonica canonica.
- Autor desta atualizacao: CODEX.

## Sistema de Subagentes - Integracao cientifica
- Pesquisei referencias primarias e de alta autoridade sobre sistemas multiagente: OpenAI, Anthropic, Microsoft Agent Framework, AutoGen e LangGraph.
- Consolidei isso em um sistema operacional de subagentes no repo.
- Arquivos criados:
  - `.brain-orchestrator/OPERACAO_AGENTES.md`
  - `.brain-orchestrator/SUBAGENT_REGISTRY.json`
  - `.brain-orchestrator/SUBAGENT_PROMPTS.md`
  - `.brain/knowledge/subagent-scientific-operating-system.md`
  - `scripts/subagent-fabric.mjs`
- Integracao adicional em indices, patterns e decisions realizada.
- Script validado com geracao automatica de time em `artifacts/subagent-fabric-*`.
- Estado vigente: CODEX ou KIMI podem operar como agente principal e gerar equipes especializadas com nomes aleatorios, contratos MAMIS/1 e ownership claro.
- Autor desta atualizacao: CODEX.
- Runtime do agente principal adicionado em `.brain-orchestrator/PRINCIPAL_AGENT_RUNTIME.md`.
- `scripts/subagent-fabric.mjs` agora gera tambem `PRINCIPAL_BRIEF.md`, permitindo que CODEX ou KIMI usem a mesma equipe com o mesmo protocolo e a mesma ordem operacional.

## Brain principal canonico
- Criado `scripts/sync-principal-brain.mjs` para espelhar `.brain` e `.brain-orchestrator` no brain principal `C:\Users\Administrator\Documents\.brain`.
- Novo script de pacote: `npm run brain:sync:principal`.
- Regra vigente: o brain principal e independente e deve receber commit em toda atualizacao significativa.
- Push automatico so acontecera quando houver remote configurado; ausencia de remote passa a ser bloqueio visivel.
- Autor desta atualizacao: CODEX.

- Governanca do brain principal adicionada com watch mode, changelog global e index global de projetos.
- Novos arquivos: `.brain/knowledge/principal-brain-governance.md`, `.brain-orchestrator/BRAIN_SYNC_RUNTIME.md`, `scripts/watch-principal-brain-sync.mjs`.
- O sync principal foi expandido para manter `PROJECTS.json` e `CHANGELOG.md` no brain principal.
- Autor desta atualizacao: CODEX.

- Repo remoto do brain principal criado e conectado: `Jhin1v9/principal-brain`.
- Push inicial do brain principal validado em `origin/main` via SSH.
- O fluxo desejado pelo usuario agora ficou operacional: atualizar brain do projeto -> sync para brain principal -> commit -> push.
- Autor desta atualizacao: CODEX.
